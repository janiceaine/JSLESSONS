// # Below is a person object, follow the directions to complete the lesson​
const person = {
  name: "Lebron",
  age: 35,
  team: "Lakers",
  occupation: "basketball player"
};
// # Get all keys from the object and assign it to the variable​
const object_keys = Object.___(____);
// # Get all values from the object and assign it to the variable​
const object_values = Object.___(____);
// # Print the keys and values to the console​
console.log(`The keys are: ${____}`);
console.log(`The values are: ${____}`);