// # Iterate over the object and print the key and value to the console.​
const person = {
  name: 'Po',
  age: 30,
  occupation: 'Kung Fu Master',
  location: 'Valley of Peace'
};

// # Iterate over the object and print the key and value to the console below:​
Object.___(____).forEach(([___, ___]) => {
  console.log(`Key: ${___}, Value: ${___}`);
});