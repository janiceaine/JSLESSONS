// # Use the spread operator to combine arr1 and arr2 into arr3 then print arr3 to the console​
const arr1 = [1, 2, 3, 4, 5];
const arr2 = [6, 7, 8, 9, 10];
const arr3 = []; // # Use the spread operator to combine arr1 and arr2 into arr3
console.log(`Result from combining arrays: ${____}`);